# Serverless Deployment Requirements
# For Modal (GPU) + Railway/Render (API Gateway)

# API Gateway (Railway/Render)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0
pydantic>=2.5.0
python-jose[cryptography]>=3.3.0

# Optional: Redis backend (use Upstash)
redis>=5.0.0

# Monitoring
sentry-sdk[fastapi]>=1.38.0
prometheus-client>=0.19.0

# For local development
python-dotenv>=1.0.0
